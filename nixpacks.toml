[phases.setup]
nixPkgs = ["php81" , "composerPackages.composer"]

[phases.install]
cmds = ["composer install --no-dev --no-interaction"]

[phases.build]
cmds = ["php artisan config:cache"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
